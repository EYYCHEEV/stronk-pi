Warning: No models match pattern "neuralwatt/glm-5.2"
Warning: No models match pattern "neuralwatt/glm-5.2-short"
---

## Smoke Test Result: **SEND_INPUT DELIVERY FAILURE**

### Step-by-step results:

| Step | Action | Result |
|------|--------|--------|
| 1 | Spawn executor | `childId`: `sp-child-3c850f34-a110-4dc8-abb9-d8402421fa11`, status `running` |
| 2 | Send `FOLLOWUP_PING` | `inputAccepted=true`, `inputLinkedChildId=sp-child-3c850f34-a110-4dc8-abb9-d8402421fa11` |
| 3 | Validate send_input | `inputAccepted=true` ✓, `inputLinkedChildId` matches ✓ |
| 4 | Wait child | Completed, terminal |
| 5 | Check output | **FAIL**: Preview shows `FOLLOWUP_CONSUMED=false`, no `STATUS_TOKEN` |
| 6 | Close child | `cleanupVerified=true` |

### Root cause:

The `send_input` API returned `inputAccepted=true` and correctly linked to the child, but the child **never received the message**. The child output shows it tried to check `intercom` for pending messages, but the `intercom` tool was **denied** in the child session (`unknown tool denied by default: intercom`). The `send_input` mechanism appears to use intercom for delivery, and when intercom is unavailable in the child context, the message is silently dropped.

### Required tokens:

```
STATUS_TOKEN=STRONK_PI_SEND_INPUT_DELIVERY_FAILED
DIAG_TOKEN=SEND_INPUT_ACCEPTED_BUT_CHILD_DID_NOT_RECEIVE
FOLLOWUP_CONSUMED=false
aliasResolved=false
recommendedNextAction=close_child
```

### Child raw output:

```
FOLLOWUP_CONSUMED=false
```
STRONK_PI_SMOKE_EXIT=0
